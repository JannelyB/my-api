/* const assert = require('chai').assert; // Importa la función 'assert' de la librería Chai para realizar aserciones en las pruebas

function addValues(a, b) {
    return a + b; // Función que suma dos valores y devuelve el resultado
}

describe('Suit de pruebas para la API', () => {
    it('Esperamos un 10', () => {
        let data = addValues(5, 5); // Llama a la función addValues con los argumentos 5 y 5, y almacena el resultado en la variable 'data'
        assert.equal(data, 10); // Realiza una aserción para verificar que el valor de 'data' es igual a 10
    });
});

*/